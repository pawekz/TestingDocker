properties:
  managedEnvironmentId: /subscriptions/#{SUBSCRIPTION_ID}#/resourceGroups/testingdocker/providers/Microsoft.App/managedEnvironments/tooltrack-env-sea
  configuration:
    ingress:
      external: true
      targetPort: 8080
    resources:
      cpu: 1
      memory: 2Gi
    #environmentVariables:
    #  - name: DATABASE_URL
    #    value: ${{ secrets.DATABASE_URL }}
